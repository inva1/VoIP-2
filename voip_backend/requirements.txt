# Flask and related extensions
Flask>=2.0.0
Flask-SQLAlchemy>=2.5.0
Flask-Migrate>=3.0.0  # For database migrations
Flask-Cors>=3.0.0     # For Cross-Origin Resource Sharing
Flask-JWT-Extended>=4.0.0 # For JWT authentication
Flask-Bcrypt>=1.0.0 # For password hashing (or use werkzeug.security)

# Database driver
psycopg2-binary>=2.9.0 # PostgreSQL adapter

# WSGI Server
gunicorn>=20.0.0

# Process Manager (not a pip install, but for server setup)
# supervisor

# Caching and Task Queue
redis>=4.0.0         # Redis client
# celery>=5.0.0      # If using Celery for background tasks (optional based on full design)

# Utility libraries
python-dotenv>=0.19.0 # For loading .env files (useful for development)
requests>=2.25.0      # For making HTTP requests (e.g., to KYC/SMS providers)
boto3>=1.20.0         # AWS SDK for Python (e.g., if using S3 for file uploads, SQS, etc.)
cryptography>=3.4.0   # For encryption, if needed beyond JWT/TLS

# Add other specific dependencies as features are built out
# e.g., for specific KYC providers, SMS providers, etc.

# For development and testing (optional to list here, can be in a dev-requirements.txt)
# pytest
# pytest-flask
# coverage

# Note: This list is based on the document's "Install dependencies" section:
# pip install flask flask-sqlalchemy flask-migrate flask-cors
# pip install psycopg2-binary redis celery
# pip install gunicorn supervisor (supervisor is not a pip package)
# pip install cryptography pyjwt (pyjwt is covered by Flask-JWT-Extended)
# pip install requests boto3
#
# Some versions are pinned to ensure compatibility or features.
# It's good practice to regularly update dependencies and test.
# Consider using a tool like `pip-tools` (pip-compile) to manage dependencies.
