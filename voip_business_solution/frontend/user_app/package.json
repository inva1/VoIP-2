{
  "name": "voip-user-app-enhanced",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend user application for the VoIP Business Solution.",
  "author": "Manus AI (Conceptual)",
  "license": "UNLICENSED",
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.2.0",   возглавляющий "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.4.5",
    "react-scripts": "5.0.1",
    "socket.io-client": "^4.5.4",  // For WebSocket communication
    "web-vitals": "^2.1.4"
    // Add other necessary dependencies here:
    // - State management (e.g., redux, zustand)
    // - UI libraries (e.g., @mui/material, antd)
    // - Date/time libraries (e.g., date-fns, moment)
    // - WebRTC helper libraries (e.g., simple-peer, react-native-webrtc for web if applicable)
    // - Styling libraries (e.g., styled-components, emotion)
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint src/**/*.{js,jsx,ts,tsx,json}",
    "lint:fix": "eslint --fix src/**/*.{js,jsx,ts,tsx,json}",
    "format": "prettier --write src/**/*.{js,jsx,ts,tsx,css,md,json} --config ./.prettierrc.json"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint": "^8.29.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "eslint-plugin-react": "^7.31.11",
    "eslint-plugin-react-hooks": "^4.6.0",
    "prettier": "^2.8.1",
    "http-proxy-middleware": "^2.0.6" // For setting up proxy during development
  },
  "proxy": "http://localhost:5000" // Default proxy for API requests if backend runs on 5000
                                  // Adjust if your backend runs on a different port/host during dev
}
